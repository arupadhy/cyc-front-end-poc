#PreRequisites
node(> v4.4.0) must be installed locally.

#Steps to start the web server in DEV env

1) cd to the root folder from a command prompt
2) npm install (This should install all necessary artifacts)
3) npm start (should start the local web server and open it in default browser)